name: Test Pipeline

on:
  push:
    branches: [ "main" ]

jobs:
  check-connection:
    runs-on: self-hosted
    steps:
      - name: Saludo
        run: Write-Host "¡Hola! Si lees esto, el Runner funciona."
